export class Tile {
    constructor() {
        this.image = null;        // Will hold an instance of Image
        this.collision = false;   // Default collision value
        this.safe = true;         // Default safe value
    }
}
